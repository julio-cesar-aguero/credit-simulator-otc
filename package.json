{
  "name": "credit-simulator-otc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "test": "web-test-runner \"src/pages/**/*.test.js\" --node-resolve --playwright --browsers chromium",
    "test:watch": "web-test-runner \"src/**/*.test.js\" --node-resolve --watch",
    "test:coverage": "web-test-runner \"src/**/*.test.js\" --node-resolve --coverage"
  },
  "dependencies": {
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "@open-wc/testing": "^3.1.6",
    "@web/test-runner": "^0.14.0",
    "@web/test-runner-playwright": "^0.11.1",
    "gh-pages": "^6.3.0",
    "sinon": "^15.0.0",
    "vite": "^7.2.4"
  }
}
